/* Grafana Alloy Configuration
 * ---
 * Logs: per-container via Docker API + system/journal -> Loki
 * Metrics: node (node_exporter-equivalent) + cAdvisor -> exposed for Prometheus scrape
 *
 * Updated to follow current Alloy docs:
 * - Use loki.relabel for log relabeling rules
 * - Remove deprecated/unused fields from loki.write
 * - Prefer explicit procfs/sysfs/udev paths for unix exporter in containers
 */

// ============================================================================
// REMOTE ENDPOINTS
// ============================================================================

/* Loki Write Endpoint */
loki.write "default" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}

// ============================================================================
// LOG COLLECTION
// ============================================================================

/* Docker Container Logs */
discovery.docker "dockerlogs" {
  host = "unix:///var/run/docker.sock"
}

// Modern pattern: define log relabel rules via loki.relabel
loki.relabel "dockerlogs" {
  // Using only exported rules; no forwarding here
  forward_to = []

  rule {
    source_labels = ["__meta_docker_container_name"]
    regex         = "/(.*)"
    target_label  = "service_name"
  }
}

loki.source.docker "default" {
  host         = "unix:///var/run/docker.sock"
  targets      = discovery.docker.dockerlogs.targets
  labels       = {"platform" = "docker"}
  relabel_rules = loki.relabel.dockerlogs.rules
  forward_to   = [loki.write.default.receiver]
}

/* System Logs & Journal */
// Relabel journald metadata using loki.relabel (recommended)
loki.relabel "journal" {
  forward_to = []
  rule {
    source_labels = ["__journal__systemd_unit"]
    target_label  = "unit"
  }
  rule {
    source_labels = ["__journal__boot_id"]
    target_label  = "boot_id"
  }
  rule {
    source_labels = ["__journal__transport"]
    target_label  = "transport"
  }
  rule {
    source_labels = ["__journal_priority_keyword"]
    target_label  = "level"
  }
}

loki.source.journal "journal" {
  max_age       = "24h0m0s"
  relabel_rules = loki.relabel.journal.rules
  forward_to    = [loki.write.default.receiver]
  labels        = {component = string.format("%s-journal", constants.hostname)}
  path          = "/var/log/journal"
}

local.file_match "system" {
  path_targets = [{
    __address__ = "localhost",
    __path__    = "/var/log/{syslog,messages,*.log}",
    instance    = constants.hostname,
    job         = string.format("%s-logs", constants.hostname),
  }]
}

loki.source.file "system" {
  targets    = local.file_match.system.targets
  forward_to = [loki.write.default.receiver]
}

// ============================================================================
// METRICS COLLECTION
// ============================================================================

/* Docker Container Metrics (cAdvisor) */
prometheus.exporter.cadvisor "dockermetrics" {
  docker_host      = "unix:///var/run/docker.sock"
  storage_duration = "5m"
}

/* System (Node) Metrics */
prometheus.exporter.unix "metrics" {
  disable_collectors = ["ipvs", "btrfs", "infiniband", "xfs", "zfs"]
  enable_collectors  = ["meminfo"]
  // Container-friendly paths (host rootfs and sysfs are mounted in docker-compose)
  procfs_path    = "/rootfs/proc"
  sysfs_path     = "/sys"
  udev_data_path = "/run/udev/data"

  filesystem {
    fs_types_exclude     = "^(autofs|binfmt_misc|bpf|cgroup2?|configfs|debugfs|devpts|devtmpfs|tmpfs|fusectl|hugetlbfs|iso9660|mqueue|nsfs|overlay|proc|procfs|pstore|rpc_pipefs|securityfs|selinuxfs|squashfs|sysfs|tracefs)$"
    mount_points_exclude = "^/(dev|proc|run/credentials/.+|sys|var/lib/docker/.+)($|/)"
    mount_timeout        = "5s"
  }
  netclass {
    ignored_devices = "^(veth.*|cali.*|[a-f0-9]{15})$"
  }
  netdev {
    device_exclude = "^(veth.*|cali.*|[a-f0-9]{15})$"
  }
}

// Scrape exporters internally and forward to Prometheus (remote write)
prometheus.scrape "node_exporter" {
  targets      = prometheus.exporter.unix.metrics.targets
  scrape_interval = "15s"
  forward_to   = [prometheus.remote_write.mimir.receiver]
}

prometheus.scrape "cadvisor" {
  targets        = prometheus.exporter.cadvisor.dockermetrics.targets
  scrape_interval = "15s"
  forward_to     = [prometheus.remote_write.mimir.receiver]
}

prometheus.remote_write "mimir" {
  endpoint {
    url = "http://mimir:9009/api/v1/push"
  }
}
